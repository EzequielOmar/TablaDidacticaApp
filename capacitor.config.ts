import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PequeñoMundo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
